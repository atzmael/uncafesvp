const soundHandler = (sound) => {
	const playSound = (globalTime) => {
		checkSoundTimeline(globalTime, sound.time);
	}
	const checkSoundTimeline = (globalTime, soundTime) => {

	}
	const loopSound = () => {

	}
};